# The current portfolio frontend requires no environment variables.
# Contact, résumé, repository, and social actions use verified direct links.
#
# If a future feature reconnects to the preserved API, Vite-exposed variables
# must use the VITE_ prefix, for example:
# VITE_API_URL=http://localhost:5000/api
